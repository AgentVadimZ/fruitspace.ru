import {Callout} from "nextra/components";


# Ⓜ️ Команды модераторов GDPS
Итак, мы с вами научились создавать роли, давать им права и кушать тортики! Но есть вопрос, а как использовать эти команды?🤔

<Callout emoji="" type="info">
    ### Передайте это модераторам и вообще всем своим игрокам, чтобы были в курсе
    Команды пишутся в комментариях к уровню. В случае успеха команда выполнится, в случае ошибки вы увидите уведомление об ошибке. Если у вас нет необходимых прав модератора, то команда будет сохранена как комментарий.
</Callout>

> Ниже приведен полный список всех команд. В скобках будет указываться необходимая привилегия роли.
> Обозначения: `<необходимый аргумент> [опциональный аргумент]`.
>
> Для выполнения **!lvl**-команд у модератора необходима привилегия **cLvlAccess**

## Список доступных команд
### Может выполнить только владелец уровня:
- `!song <id> [ng]` - Разрешает владельцу уровня менять саундтрек на встроенный в игру. Добавьте "ng" чтобы использовать кастомную музыку.

- `!lvl copy <on/off/pass> [pass]` - позволяет разрешать/запрещать свободное копирование. Если использовать pass, то необходимо будет указать пароль.

### Может выполнить только модератор:
- `!feature и !unfeature` - дает уровню feature и убирает соответственно (cFeature)

- `!epic и !unepic` - дает уровню epic и убирает соответственно (cEpic)

- `!coins <verify/reset>` - используйте verify, чтобы подтвердить монеты и reset, чтобы снять подтверждение (cVerCoins)

- `!rate <diffName/reset>` - оценивает уровень, но не дает ему звёзд. Используйте auto, easy, normal, hard, harder, insane чтобы задать сложность и reset для сброса до N/A (cRate)

- `!daily [reset]` - делает уровень уровнем дня, а если вы добавить reset, то отменяет статус уровня дня (cDaily)

- `!weekly [reset]` - аналогично, только для уровней недели (cWeekly)

- `!lvl delete <level_ID>` - удаляет уровень, необходимо указать id уровня, чтобы подтвердить серьезность намерений (cDelete)

### Может выполнить и модератор, и владелец уровня:
- `!lvl rename <name>` - позволяет переименовать уровень. Пробелы разрешены.

- `!lvl chown <level_ID> <new_Uname>` - меняет владельца уровня, необходимо указать id уровня для подтверждения намерений и имя пользователя, которому необходимо передать уровень.

- `!lvl desc <description>` - меняет описание уровня, пробелы разрешены

- `!lvl <list/unlist>` - убирает или добавляет метку Unlisted уровню. Влияет на отображение в поиске
